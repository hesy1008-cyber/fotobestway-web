# Fotobestway 网站 - 新对话交接文档

## 项目基本信息
- 路径：`C:\Users\FBW\fotobestway-web`
- 技术栈：Next.js 16 + React 19 + Prisma + SQLite + TypeScript
- **注意：Next.js 16 有 breaking changes，写代码前查 `node_modules/next/dist/docs/`**
- 系统：Windows，Node 路径 `f:\node.exe`

## 当前运行状态（都正常）
- 运行模式：**生产模式**（`npm run build` 后 `npm start`），响应约 47ms
- 看门狗：`watchdog.js`（生产模式版）后台运行，端口3000卡死自动重启
  - 每25秒检查，连续3次失败(约75秒)重启，10分钟最多5次；只杀端口3000进程；DEV_MODE=false
  - 启动：`node watchdog.js`
- 后台账号：admin / fotobestway2026（HTTP Basic Auth）
- 后台地址：http://192.168.10.108:3000/admin

## 后台当前结构（app/admin/）
导航已整合为 **3 大类**（`app/admin/layout.tsx`，客户端点击展开下拉）：
1. **控制台** `/admin`（app/admin/page.tsx）—— 统计卡片 + **客户询盘区**（Inquiries已并入，用 InquiryList 组件）
2. **产品管理**（下拉）：产品 `/admin/products`、分类 `/admin/categories`
3. **页面内容**（下拉）：首页内容 `/admin/media`、关于页 `/admin/about`
- 询盘独立页 `/admin/inquiries` 仍在（InquiryList.tsx 在此），导航不再单列

## 待办（用户下一步想要的）
**把顶部下拉导航改成「左侧边栏」布局**：
- 左侧竖向固定侧边栏，子项常驻显示，不用展开收起
- 菜单：控制台 / 产品管理(产品、分类) / 页面内容(首页内容、关于页)
- 当前页红色高亮，品牌色 #e60012 红 + 黑 + 白
- 用户要求：先出设计图看，再动手，动手前先备份
- 主要改 `app/admin/layout.tsx`（+可能 admin.css），不动各列表页

## 其他已完成
- 联系表单真实提交存库（Inquiry 表 + actions/inquiry.ts）
- 首页轮播接数据库Banner（无数据用 studio.jpg 兜底）
- 产品详情页SEO（generateMetadata）；加了 sitemap.ts/robots.ts（上线前在.env设SITE_URL）
- 补全产品短描述

## 已知小问题（非bug）
- Fresnel Light 产品图配错（用了PL-400B图），需后台换图
- 数据库有测试产品「一个小灯架/Test upload product」，用户说先不删
- admin目录有大量历史备份垃圾文件（.backup/.bak/.old等几十个），可清理

## 备份位置
- 看门狗原版：`watchdog-backup-original.js`
- 后台改动备份：`admin-backup-<时间戳>/` 目录

## 注意事项
- 改代码后需重新 build 才生效；改完 `taskkill /F /IM node.exe` 再 `node watchdog.js`
- 这是本地 Next 项目，不是飞书文档
- 构建/重启先停 node 再 build，避免冲突
